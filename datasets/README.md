Place your test `.pcap` files here. 

For Phase 2 testing, please download a sample PCAP (for instance, from Wireshark sample captures or malware-traffic-analysis.net) and save it as `sample.pcap` in this directory.

Then, you can run the test script:
`python tests/test_ingestion.py datasets/sample.pcap`
